---
// Astro component
import list from "./sidebar.json";
import MenuItem from "./MenuItem.astro";
---

<aside class="sidebar">
    {list.map((item) => (
        <MenuItem item={item} level={1} />
    ))}
</aside>

<style>
  .sidebar {
    width: 200px;
  }
</style>

